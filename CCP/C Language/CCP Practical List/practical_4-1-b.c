#include <stdio.h>
void main()
{
    int no;

    printf("\n Enter decimal number :");
    scanf("%d", &no);

    printf("\n Hexadecimal of %d is %x", no, no);
    printf("\n Octadecimal of %d is %o", no, no);
}
